- name: Django Box
  vars:
    user_home: /home/vagrant/
    num_executors: 1
    django_user: vagrant
  hosts: all
  roles:
    - browsers
    - djangodata
    - memcached
    - mysql
    - ntp
    - postgres
    - python
